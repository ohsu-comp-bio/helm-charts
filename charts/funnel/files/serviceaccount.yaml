apiVersion: v1
kind: ServiceAccount
metadata:
  name: funnel-worker-sa-{{.TaskId}}
  namespace: {{.Namespace}}
  labels:
    app: funnel
    taskId: {{.TaskId}}
spec:
  # TODO: Should tokens be automatically mounted into pods?
  automountServiceAccountToken: false
